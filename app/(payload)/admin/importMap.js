// No custom admin UI components configured yet, so nothing needs to be
// registered here. Payload's built-in field/view components resolve on
// their own without an entry in this map.
export const importMap = {}
